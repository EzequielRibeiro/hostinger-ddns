# Contributing

Contributions are welcome.

Before opening a pull request:

1. Run `bash -n hostinger-ddns install.sh uninstall.sh`.
2. Run ShellCheck on the scripts.
3. Never commit a real Hostinger API token.
4. Keep changes compatible with systemd-based Linux installations.
5. Document behavior changes in `CHANGELOG.md` when appropriate.
